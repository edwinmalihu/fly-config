# fly.toml app configuration file generated for customer-service-edwin on 2025-07-04T00:07:50+07:00
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = '{APPNAME}'
primary_region = 'sin'

[build]
  image = '{IMAGE}'

[env]
  SERVER_PORT = '8080'

#[http_service]
#  internal_port = 8080
#  force_https = true
#  auto_stop_machines = 'stop'
#  auto_start_machines = true
#  min_machines_running = 0
#  processes = ['app']


[[vm]]
  memory = '1gb'
  cpu_kind = 'shared'
  cpus = 1
